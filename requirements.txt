# Core UI
streamlit>=1.50,<2
# IMPORTANT: keep altair on the 5.x line. Altair 6.x emits Vega-Lite v6
# specs, but Streamlit's bundled chart renderer only understands v5 —
# bar charts render axes/labels but no bars if you let this drift to 6.x.
altair>=5.0,<6

# Data
pandas>=2.2
openpyxl>=3.1

# Agent stack (LangGraph + LangChain, local Ollama model)
langchain>=0.3
langchain-core>=0.3
langchain-groq>=0.3
langgraph>=0.2
